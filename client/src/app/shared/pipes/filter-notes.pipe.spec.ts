import { FilterNotesPipe } from './filter-notes.pipe';

describe('FilterNotesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterNotesPipe();
    expect(pipe).toBeTruthy();
  });
});
